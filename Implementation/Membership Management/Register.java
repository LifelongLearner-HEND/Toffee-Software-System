package MembershipManager;

import static CustomerService.Database.Registered_Users;

public class Register {

    // make the user register and then push their data to the database
//    public void register(RegisteredCustomer customer) {
//        // check if the id already exists in the database
//        if (Registered_Users.containsKey(customer)) {
//            System.out.println("User already exists");
//        } else {
//            Registered_Users.put(customer, customer.getPassword());
//            System.out.println("User registered successfully");
//        }
//    }
}
